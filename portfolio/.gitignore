# Sensitive credentials — never commit these
php/config.php

# Profile images — keep private
images/profile1.JPG
images/profile2.JPG

# Composer dependencies — run "composer install" on the server instead
vendor/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# PHP error logs
*.log
error_log
